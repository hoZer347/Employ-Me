# Netlify build & deploy configuration
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for any route so client-side
# navigation works on direct loads / refreshes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
